# Spec-Driven Development: Implementation Phase
When the user invokes `/speckit.implement`, execute the following sequential steps:
1. Read the approved `plan.md` file from the workspace.
2. Break down the plan into atomic coding tasks.
3. Execute the code generation step-by-step.
4. Verify the changes using the terminal (e.g., running linting or tests).
