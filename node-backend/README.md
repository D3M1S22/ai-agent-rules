# ⚙️ Node.js Backend Agent Context

Context payload optimized for robust, secure, and scalable Node.js REST APIs.

**Key Paradigms Enforced:**
- **Architecture**: Strict Separation of Concerns (Controllers -> Services -> Repositories).
- **Validation**: Schema-driven request validation using `Zod`.
- **Database**: ORM standards (Prisma/Drizzle), indexing constraints, and migration rules.
- **Skills included**: Includes the `db-architect` and `docker-nix-ops` progressive disclosure skills for Antigravity.
